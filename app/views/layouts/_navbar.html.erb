<nav class="navbar" role="navigation" aria-label="main navigation">
  <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navMenu">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>
  <div class="navbar-menu" id="navMenu">
    <div class="navbar-start">
      <% if logged_in? %>
        <a href="<%= character_path(@character) %>" class="navbar-item">
          Home
        </a>

        <a href="<%= houses_path %>" class="navbar-item">
          Houses
        </a>

        <a href="<%= spells_path %>" class="navbar-item">
          Spells
        </a>
      <% end %>
    </div>
    <div class="navbar-end">
      <div class="navbar-item">
        <% if logged_in? %>
          <a href="<%= logout_path %>" class="button">Log out</a>
        <% else %>
        <div class="buttons">
        <a href="<%= signup_path %>" class="button is-primary">
          <strong>Sign up</strong>
        </a>
        <a href="<%= login_path %>" class="button is-light">
          Log in
        </a>
        </div>
        <% end %>
      </div>
    </div>

  </div>
</nav>
